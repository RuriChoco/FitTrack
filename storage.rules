rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow anyone to view images, but only authenticated users can upload them
    match /avatars/{imageId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}